# full-pipeline branch: the complete vendored miso pipeline.
# Core (required):
azure-ai-documentintelligence
azure-core
anthropic
python-dotenv
pillow
pdf2image          # render PDF pages to images (needs the system 'poppler' binary)
wordfreq
pylatexenc
sentence-transformers
reportlab          # --format pdf: lay the note out as a PDF with figure diagrams

# Figure pass renders each figure's Mermaid to a PNG via the Mermaid CLI (mmdc),
# a Node tool — NOT pip-installable. Install separately for PNG/PDF/Docs figures:
#   npm install -g @mermaid-js/mermaid-cli
# (needs a headless Chromium; on restricted hosts add a puppeteer-config.json with
#  {"args": ["--no-sandbox"]} in the repo root — mmdc picks it up automatically).
# Without mmdc, HTML/Markdown still render figures (mermaid.js / ```mermaid fence);
# only the rasterized PNG embed (PDF, Google Docs) is skipped.

# Open VLMs via OpenRouter / any OpenAI-compatible endpoint (eval suite).
# Without this, _make_extractor SILENTLY falls back to the StubExtractor for any
# non-claude model id — runs "succeed" but echo OCR instead of calling the VLM.
openai

# Google Docs export (only needed for --drive):
google-api-python-client
google-auth-oauthlib
google-auth-httplib2

# Optional: faster on-disk vector index.
# sqlite-vec
