# full-pipeline branch: the complete vendored miso pipeline.
# Core (required):
azure-ai-documentintelligence
azure-core
anthropic
python-dotenv
pillow
wordfreq
pylatexenc

# Google Docs export (only needed for --drive):
google-api-python-client
google-auth-oauthlib
google-auth-httplib2

# Optional: dense retrieval + cross-encoder rerank (heavy; BM25-only without them).
# sentence-transformers
# sqlite-vec
