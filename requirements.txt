# eval branch: full pipeline + corpus/eval tooling.
# Core pipeline:
azure-ai-documentintelligence
azure-core
anthropic
python-dotenv
pillow
wordfreq
pylatexenc

# Google Docs export:
google-api-python-client
google-auth-oauthlib
google-auth-httplib2

# Retrieval (needed to evaluate the retrieval cache):
sentence-transformers

# Corpus staging (download + render HuggingFace note datasets):
datasets
pymupdf

# Optional: faster on-disk vector index.
# sqlite-vec
